#!/usr/bin/env bash

CHECK=$(ifconfig tun0)

if [ $? -eq 0 ]; then
  # vpn is up, let's stop it
  sudo systemctl stop openvpn-client
else
  # vpn is down, let's start it
  sudo systemctl start openvpn-client
fi

